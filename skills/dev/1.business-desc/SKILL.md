
- 写在`docs/entity/`目录下
 - 一个实体一个md文件
 - 文件名：实体名.md

- 实体的介绍
- 实体有的属性
- 与其它实体的关系


## 实体属性命名规范
- 下划线命名
- 每个命名由 多个前缀 + 多个词根 + 多个后缀拼接而成
- 请维护一个前缀, 词根, 后缀的表格, 写在`docs/naming.md`
  - 前缀: 前缀用于修饰,如: live_(直播的), video_(视频的), pre_(前置的)
  - 后缀: 后缀用于定性, 如: _cnt(统计), _inc(增量), _total(总量). _avg(平均值)
  - 词根: 词根用于描述, 如: user(用户), order(订单), product(产品)



