---
name: business-desc
description: 当用户需要进行业务实体建模, 对业务实体进行描述时使用。
---

# 核心目标
建立统一、可维护、可扩展的业务实体文档体系，确保：
- 实体定义清晰
- 属性语义统一
- 命名规则标准化
- 实体关系明确
- 前后端/数据库/API 使用一致
- 支持后续建表、接口设计、代码生成


# 我的要求
- 列出各个实体的命名, 询问我是否需要修复命名
- 依据`@docs/prototype`


# 一、文档目录规范

## 实体文档目录
- 所有实体文档统一存放于：`docs/entity/`
- 一个实体一个 Markdown 文件
- 文件命名规则：
  - `实体名.md`
  - 使用业务英文名，采用 UpperCamelCase
  - 示例：`User.md`

## 公共规范文档
- `docs/naming.md`：属性命名规范（前缀 / 词根 / 后缀）
- `docs/entity/index.md`：实体总目录（实体清单 + 关系总览）
- `docs/entity/enum.md`：枚举值统一定义
- `docs/sql/实体.sql`：实体建表语句


# 二、单个实体文档标准结构（强制）


# 实体名称（中文 + 英文）

## 1. 实体定义
- 实体名称：
- 英文名称：
- 核心作用：
- 所属业务域：
- 实体说明：
- 是否核心实体：
- 生命周期：

## 2. 业务场景
- 创建场景：
- 修改场景：
- 删除场景：
- 查询场景：
- 状态流转：

## 3. 属性
- 生成建表语句到`@docs/sql`里

| 属性名 | 中文名 | 类型 | 必填 | 默认值 | 说明(中文) |
|--------|--------|------|------|--------|------|


## 4. 核心主键
- 主键：
- 唯一键：
- 业务唯一键：
- 外键：

## 5. 实体关系
| 关系类型 | 目标实体 | 关系说明 |
|----------|----------|----------|
| 1:1      |          |          |
| 1:N      |          |          |
| N:N      |          |          |

## 状态字段
| 状态字段 | 状态值 | 含义 |

## 数据库映射建议
- 表名：
- 索引建议：
- 分区建议：


# 三、实体设计原则

## 必须包含
### mysql
* id（主键）
* created_at（创建时间）
* updated_at（更新时间）

### clickhouse
* created_at（创建时间）



# 四、实体关系设计规范

## 关系分类

* One-to-One（1:1）
* One-to-Many（1:N）
* Many-to-Many（N:N）
* 聚合关系（Aggregation）
* 组合关系（Composition）


# 禁止事项

* 禁止同义字段并存：

  * ❌ user_id / uid
* 禁止时间歧义：
  * ❌ time
  * ✅ created_at
* 禁止布尔模糊：
  * ❌ enable
  * ✅ is_enabled

# 交付物
* 实体文档（docs/entity/*.md）
* 实体建表语句（docs/sql/*.sql）


# 最终目标
通过该规范可直接支撑：
* 原型分析
* 数据库建模
* 建表 SQL
* OpenAPI DTO
* 前端 TypeScript Interface
* 后端 Entity / VO / DTO
* 数据仓库指标字段统一

